# Changelog

## 0.1.0

- Initial RustFerry project discovery, IDE protocol client, native views, commands, tasks, diagnostics, project wizard, capability management, snippets, and VSIX packaging.
- Add a workspace-bound Jobs tree with strict, exact-handshake-gated job and artifact receipts, bounded log paging and follow, remote snapshot consent previews, and signing readiness.
- Normalize Windows extended-length diagnostic paths so unsaved `ferry.toml` validation attaches to the active editor.
